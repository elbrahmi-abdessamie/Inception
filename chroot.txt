apt-get update
apt-get install debootstrap
mkdir -p /srv/chroot/debian
debootstrap bullseye /srv/chroot/debian http://deb.debian.org/debian

chroot .
/# install docker engine #/
apt update
apt -y install apt-transport-https ca-certificates curl gnupg-agent software-properties-common

curl -fsSL https://download.docker.com/linux/debian/gpg | apt-key add -

sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/debian $(lsb_release -cs) stable"


sudo apt update

apt -y install docker-ce docker-ce-cli containerd.io

* Create selfsigned certificate
openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -sha256 -days 3650 -nodes -subj "/C=XX/ST=Morocco/L=Khoribga/O=42/OU=1337/CN=localhost"

https://github.com/codesshaman/inception/blob/main/03_CLOUD_STORAGE.md